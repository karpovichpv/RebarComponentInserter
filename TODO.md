1. Add on the main form a possibility to set a certain component from the catalog
2. Send from the form to the plugin the name of the component that a user choosed
3. Offsets
    - set offsets from start and end points
    - set offsets from plane
4. Dropdown list for spacing types
    - add on the form
    - send the right type in a code
5. Send attributes to the component:
    - add controls on the form
    - send attribute values (WIDTH, LENGTH) in the code and write the them to the components attributes

6. Add automatic a copy process after the plugin build
7. Solve the problem with the plugin rotation when it is inserted into the wall
    - write message
    - make a plane how to solve


