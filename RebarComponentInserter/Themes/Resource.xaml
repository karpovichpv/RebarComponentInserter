<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    >

    <!-- Style for labels in the form -->
    <Style x:Key="FormLabelStyle" TargetType="Label">
        <Setter Property="Height" Value="30"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="5,0,5,0"/>
    </Style>

    <!-- Style for textboxes in the form -->
    <Style x:Key="FormTextBoxStyle" TargetType="TextBox">
        <Setter Property="Width" Value="120"/>
        <Setter Property="Height" Value="23"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="5,0,5,0"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>

    <!-- Style for textboxes with smaller width (for numbers) -->
    <Style x:Key="FormTextBoxSmallStyle" TargetType="TextBox" BasedOn="{StaticResource FormTextBoxStyle}">
        <Setter Property="Width" Value="90"/>
    </Style>

    <!-- Style for combo boxes -->
    <Style x:Key="FormComboBoxStyle" TargetType="ComboBox">
        <Setter Property="Width" Value="90"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="5,0,5,0"/>
    </Style>

</ResourceDictionary>